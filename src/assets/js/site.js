$(document).ready(function(){
  // initialize sidenav
  $('.sidenav').sidenav();

  // initialize collapsible
  $('.collapsible').collapsible();

  // initialize select
  $('select').formSelect();
});

